# Environnement Python (propre à chaque machine, cf. README)
.venv/
# Runtime portable partagé via OneDrive (binaire, ~400 Mo : jamais dans git)
runtime/
__pycache__/
*.pyc
.pytest_cache/

# Données projets (dossiers clients, jamais versionnés)
PROJETS/

# Secrets et config locale par poste (ne JAMAIS versionner)
.env
.env.*
config.local.json

# OS / OneDrive
.DS_Store
Thumbs.db
~$*
